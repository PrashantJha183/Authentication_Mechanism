import React from "react";

const MicrosoftProfile = ({ account }) => {
  return (
    <div>
      <h2>Welcome, {account.name}</h2>
      {/* Display user profile information */}
    </div>
  );
};

export default MicrosoftProfile;
